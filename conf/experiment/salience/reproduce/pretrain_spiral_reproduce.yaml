defaults:
  - /experiment/salience/spiral/pretrain_spiral@_here_
  - /experiment/salience/env_interface/res_128@config.env_interface_config
  - _self_

config:
  logging:
    run_name: pretrain_spiral_reproduce

  monty_config:
    motor_system_config:
      motor_system_args:
        policy_args:
          fixed_amount: 5

  train_env_interface_args:
    object_names: ${project.objects.distinct}