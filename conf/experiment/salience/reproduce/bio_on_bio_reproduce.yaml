defaults:
  - /experiment/salience/bio/bio@_here_
  - /experiment/salience/env_interface/res_128@config.env_interface_config
  - _self_

config:
  
  model_name_or_path: ${project.paths.pretrain_dir}/pretrain_bio_reproduce/pretrained/
  logging:
    run_name: bio_on_bio_reproduce
  
  eval_env_interface_args:
    object_names: ${project.objects.distinct}