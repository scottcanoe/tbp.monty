defaults:
  - /experiment/salience/uniform/uniform@_here_
  - _self_

config:
  
  model_name_or_path: ${project.paths.pretrain_dir}/pretrain_uniform_reproduce/pretrained/
  logging:
    run_name: uniform_on_uniform_reproduce
  
  eval_env_interface_args:
    object_names: ${project.objects.distinct}