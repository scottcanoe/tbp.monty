defaults:
  - /experiment/salience/sal/pretrain_sal@_here_
  - _self_

config:
  logging:
    run_name: pretrain_sal_10distinctobj

  train_env_interface_args:
    object_names: ${project.objects.distinct}

